# Despliegue de GLPI con Docker

Despliegue de GLPI usando contenedores Docker con MariaDB.

## Requisitos
- Docker
- Docker Compose

## Cómo ejecutar
```bash
docker compose up -d
```
Accede en: http://localhost:8080
